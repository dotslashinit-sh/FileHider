<Window x:Class="Hider.HiderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hider" xmlns:lib="clr-namespace:FileHider.Lib;assembly=FileHider.Lib" d:DataContext="{d:DesignInstance Type=lib:Session}"
        mc:Ignorable="d"
        Title="File Hider" Height="600" Width="800" Loaded="OnWindowLoaded">

    <Grid Margin="10, 10, 10, 10">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <!--File input row-->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="80"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Margin="2">File:</Label>
            <TextBox x:Name="inputFileBox"
                     Grid.Column="1" Margin="2"
                     Text="{Binding Path=InputFile, Mode=TwoWay}"></TextBox>
            <Button x:Name="brwsInputFileBtn"
                    Grid.Column="2" Margin="2"
                    Click="OnBrowseBtnClick">Browse</Button>
        </Grid>

        <!--Buttons row-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="100"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"
                        Orientation="Horizontal" >
                <Button x:Name="addFilesBtn"
                        Width="80"
                        Click="OnAddFilesBtnClick"
                        Margin="2">Add files</Button>
                <Button x:Name="addFromDirBtn"
                        Width="80"
                        Click="OnAddDirBtnClick"
                        Margin="2">Add Folder</Button>
                <Button x:Name="removeBtn"
                        Width="80"
                        Click="OnRemoveBtnClick"
                        Margin="2">Remove Item</Button>
                <Button x:Name="removeAllBtn"
                        Width="80"
                        Margin="2"
                        Click="OnRemoveAllBtnClick">Remove All</Button>
                <Button x:Name="BackBtn"
                        Width="80"
                        Click="OnBackBtnClicked"
                        Margin="2">Back</Button>
            </StackPanel>
            <Button x:Name="hFilesBtn"
                    Width="80"
                    Click="OnHideFilesBtnClick"
                    Margin="2"
                    Grid.Column="1"
                    HorizontalAlignment="Right">Hide files!</Button>
        </Grid>

        <!--Files list -->

        <Grid Grid.Row="2">
            <ListView x:Name="filesListView"
                      Margin="2"
                      MouseDoubleClick="OnFilesListDoubleClick"
                      ItemsSource="{Binding Files.CurrentDirectory.Children}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Name}"
                                        Header="Name"
                                        Width="500">
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</Window>
